# TASK-20260925-ci-release-implementation：接入 GitHub CI 与版本发布

- 记录日期：2026-09-25
- 模块与关键词：GitHub、CI、Changesets、容器、扩展、版本 PR
- 维护归属：本次实施会话；主会话维护记录、依赖和工作流
- 代码基线：本地 `main` 尚无首次提交
- 关联任务：[前期规划](TASK-20260925-github-ci-release.md)

## 背景与目标

用户确认继续前期方案。将项目已有测试接入 CI，建立版本 PR、服务端镜像和独立扩展发布流程，并在验证完成后接入 GitHub。仓库可见性、许可证及线上访问入口由用户选择；依赖这些选择的外部操作在确定后执行。

## 验收标准

- [x] 干净环境可安装依赖、检查类型、运行现有测试并构建双端。
- [x] 隔离 HTTP 联调自动准备模拟服务及临时数据，结束后清理。
- [ ] Linux 容器实际验证新装、重启持久化及可选的上版升级。
- [x] 配置普通 PR 不发布；版本 PR 的内容必须匹配 Changesets 生成结果，并测试防绕过规则。
- [x] 配置网页与扩展独立发布，并用工具测试验证失败不推进 stable、已有版本不被不同内容覆盖。
- [x] 文档说明实际启用方式、凭据边界及未验证项。

## 讨论结论

沿用既有测试集合；CI 负责环境准备与编排。服务端使用 ACR 镜像，扩展使用 GitHub Release ZIP，版本包为 `@sayseed/web` 与 `@sayseed/extension`，共享包变化按 Changesets 依赖关系传播。GitHub 不保存服务器 SSH 私钥。

并行文件分工：主会话负责工作流、依赖、Docker/Compose 和文档；联调 worker 负责 `scripts/ci-smoke.mjs`、`scripts/docker-smoke.mjs` 及测试；版本 worker 负责版本校验与扩展打包脚本及测试；发布 worker 负责 `scripts/publish-release.py` 及测试。任务文档由主会话单独维护。

## 实现结果

2026-09-25：增加 `CI`、`Version Packages`、`Publish Release` 三个工作流及 PR 模板。CI 调用现有测试，补自动隔离 HTTP 联调、Linux 容器检查、扩展打包和固定 `validate` 汇总检查；工作流使用固定 action 提交。

引入 Changesets 3.0.1。网页与扩展独立版本，共享包变化可传播两端；初始 changeset 将两端从本地基线 0.1.0 更新至 0.1.1。普通 PR 禁止手改版本、CHANGELOG 和已有 changeset；版本 PR 在临时 Git 副本重演生成过程并比较整个文件树。

增加扩展可复现 ZIP、版本元数据、更新说明与完整校验文件。发布脚本验证 Git/包/OCI 身份，先验证候选空卷新装，再按需验证上一版升级；不可变镜像推送成功并生成校验元数据后才推进 stable。GitHub Release 使用草稿上传、下载比对、再公开的顺序。重试复用已存在的同一制品，并拒绝覆盖或降级。

Node 固定为 22.23.3，Web 类型检查先生成 Next.js 类型。Dockerfile 增加版本标签，生产 Compose 使用明确镜像与固定数据卷名。忽略规则补充其他环境文件、数据库、密钥和构建产物；清理原扩展文件末尾的一处空白以满足首次提交检查。

[发布说明](../release.md)记录工作流、仓库设置、凭据接口、生产配置和恢复边界；README、文档入口、验证说明及协作规则同步增加入口。未在文档中写入私有运维地址或凭据。

## 验证事实与边界

2026-09-25：本机可用 Node.js 与 pnpm，GitHub CLI 已登录项目所有者账号；本机没有 Docker 命令。容器实际运行需要 GitHub Actions 的 Linux Docker 环境。未将本地可用环境等同于 CI 或发布已通过。

2026-09-25：从待提交源码生成独立干净副本，不包含本地环境文件、数据库、依赖或构建缓存；使用 Node 22.23.3、pnpm 12.4.1 锁文件安装后，通过类型检查、146 项测试（服务端 53、扩展 62、Node 工具 12、Python 工具 19）、网页/扩展生产构建和 86 项隔离 HTTP 检查。此前主工作区 Node 24 上的检查也通过，但固定版本的干净副本为本次主要证据。

在该一次性副本中创建测试 Git 基线，实际执行 Changesets 生成 0.1.1 版本提交，严格重演校验与组件路由通过；构建并打包 0.1.0、0.1.1 扩展候选，manifest 与包版本一致。测试副本的提交和制品不视为真实发布。

独立代码审查修正了 RELEASE_BASE 参数必须先解析为完整 SHA 的工作流问题，并补充发布时同时验证候选新装和上一版升级。Actionlint 通过；发布与打包 19 项 Python 测试覆盖失败不推镜像、版本防覆盖/降级、草稿附件校验等行为。文档本地链接和 Git 空白检查通过；待提交文件扫描未检出常见私钥或 token 格式，也没有将本地环境和数据库纳入源码。

上述发布行为属于代码及模拟验证，尚未实际运行 GitHub Actions、Linux Docker、ACR 推送或 GitHub Release。记录截至本轮本地实施，不声称外部发布成功。

## 未覆盖范围与后续建议

2026-09-25：仓库可见性、许可证及线上入口的用户选择尚未收到，因此未创建或上传 GitHub 仓库。接入后还需启用 Actions 创建 PR 权限与主分支规则，配置专用 ACR 仓库及发布/拉取权限，并核验首次 CI 与版本 PR。

真实部署入口、服务器初始化、完整备份恢复与自动升级器属于后续部署工作；本任务先提供本地验证过的发布配置与生产 Compose 入口。外部账号配置和首次发布的实际结果在完成后按日期记录，不由源码推定。旧版本整条发布重试可能被 stable 防降级规则拒绝，仅 GitHub Release 失败时应优先重跑对应 job。
