# TASK-YYYYMMDD-short-topic：标题

- 记录日期：YYYY-MM-DD（事实截至本次工作）
- 模块与关键词：
- 维护归属：负责记录的会话或工作分支
- 关联任务 / PR / 提交：

文件名保持稳定。单个任务由一个会话维护，交接时更新归属；独立工作另建任务并关联。新增任务不登记总索引。按需省略不适用章节。

## 背景与目标

说明使用场景、需求或问题。修复类任务补充复现步骤、实际行为和期望行为。

## 验收标准

- [ ] 本次工作可验证的要求。勾选只代表已经取得的证据，不代表后续 CI、合并、发布或上线。

## 讨论结论

区分用户已确认的目标与范围、已采纳的决定、推荐方案和仍需确定的问题。保留关键决定及选择理由，有实际讨论时记录重要备选方案，不补造理由；范围或决定变化时注明日期及替代的旧决定或假设，避免复制完整聊天。

## 实现结果

按日期记录实际完成的修改与行为，并注明尚未实现的方案。长期有效的项目约束与验证方法精炼到 `AGENTS.md` 或模块规则中，保留链接。

## 验证事实与边界

记录日期、代码提交或工作区基线、实际执行的方法、结果及必要证据。未运行、失败或被环境限制的检查如实说明；不将历史记录当作本轮验证。

只保留帮助理解结果的证据，不记录逐次推送、等待 CI 等操作流水。后续 PR/CI/发布状态查关联平台，实际部署查私有运维记录或现场。

## 未覆盖范围与后续建议

记录截至本次工作的未实现需求、未验证场景和建议，不视为自动跟进承诺。接续工作时补充日期和新事实，明确已经解决的旧限制。

原始日志、截图、凭据、模型调用内容和运行数据留在被 Git 忽略的本地目录或私有运维目录。
